#1436
N = int(input())
c = 0
num = 0
while c < N:
    num += 1
    if '666' in str(num):
        c += 1
print(num)

# 컴퓨터를 너무 무시한 것 같다... 그냥 1씩 더해보면 되는 것을 막 666 어떻게든 붙이는 것만 생각했다가 시간 날려먹었다
# 그리고 문자열 일때 in 쓰면 글자 수 달라도 알아서 구성요소에서 찾아 준다.
